export const docsHtml =
  '<div class="docs-progress" id="docs-progress"></div>\n<header>\n<div class="wrap">\n<nav>\n<a aria-label="SOLARD home" class="brand" href="/">\n<svg aria-hidden="true" class="mark" height="18" viewbox="0 0 32 32" width="18"><path d="M10.5 4h16l-3.4 5h-16z"></path><path d="M7.1 13.5h16l3.4 5h-16z"></path><path d="M10.5 23h16l-3.4 5h-16z"></path></svg>\n<span class="wm">SOLARD</span><span class="sfx">/docs</span>\n</a>\n<div class="nav-links"><a href="/">home</a><a href="/trade">trade</a><a href="https://github.com/7flash/solard">github</a><a href="https://x.com/solardxyz">x · @solardxyz ↗</a><a class="act" href="#quickstart">quickstart →</a></div>\n<button aria-controls="side" aria-expanded="false" class="docs-menu" id="docs-menu">menu</button>\n</nav>\n</div>\n</header>\n<div class="wrap docs">\n<aside id="side">\n<div class="docs-search"><label for="docs-search">Search docs</label><input autocomplete="off" id="docs-search" placeholder="commands, env, routes…" type="search"/><span id="docs-search-count">14 sections</span></div>\n<div class="grp">Start here</div>\n<a href="#overview">Overview</a><a href="#quickstart">Quickstart</a><a href="#configuration">Configuration</a>\n<div class="grp">System</div>\n<a href="#architecture">Architecture</a><a href="#workers">Workers</a><a href="#console">Web console</a><a href="#api">HTTP API</a><a href="#market-data">Market data</a>\n<div class="grp">Execution</div>\n<a href="#cli">CLI reference</a><a href="#venues">Venues &amp; senders</a><a href="#launching">Launching</a>\n<div class="grp">Extend</div>\n<a href="#sdk">SDK, scripts &amp; agents</a><a href="#safety">Safety &amp; operations</a><a href="#repository">Source reference</a>\n<div class="docs-shortcut"><kbd>/</kbd> focus search</div>\n</aside>\n<article>\n<section data-doc-section="" id="overview">\n<div class="eyebrow">Documentation</div>\n<h1>Solard Documentation</h1>\n<p class="lead">SOLARD is a local-first execution stack for Pump and PumpSwap. Its SOLARD trade app, <code>solard</code> CLI, and typed SDK share one SQLite database, one venue resolver, and one transaction pipeline.</p>\n<div class="status-grid"><div><span>indexer</span><b><code>bun run indexer/main.ts</code></b></div><div><span>trade app</span><b><code>bunx tradjs server</code></b></div><div><span>database</span><b><code>./solard.db</code></b></div><div><span>live gate</span><b><code>0 by default</code></b></div></div>\n</section>\n<section data-doc-section="" id="quickstart">\n<h2><span class="hash">#</span>Quickstart</h2>\n<p>The actual trade app is two long-running processes. The indexer collects and writes market state; the TradJS server reads that state and serves the browser interface. Run both from the repository root in separate shells.</p>\n<h3>1 · Clone and install</h3>\n<div class="code"><div class="bar"><span>shell</span><button data-c="git clone https://github.com/7flash/solard &amp;&amp; cd solard &amp;&amp; bun install &amp;&amp; cp .env.example .env">copy</button></div><pre><span class="c-c">$</span> git clone https://github.com/7flash/solard\n<span class="c-c">$</span> cd solard\n<span class="c-c">$</span> bun install\n<span class="c-c">$</span> cp .env.example .env</pre></div>\n<h3>2 · Configure the minimum</h3>\n<div class="code"><div class="bar"><span>.env</span><button data-c="">copy</button></div><pre><span class="c-c"># required for wallet import/decryption</span>\nSOLARD_MASTER_KEY=<span class="c-s">replace-with-a-long-secret</span>\n\n<span class="c-c"># required for chain operations and indexing</span>\nHELIUS_RPC_URL=<span class="c-s">https://mainnet.helius-rpc.com/?api-key=...</span>\n\n<span class="c-c"># keep live execution disabled while testing</span>\nSOLARD_ENABLE_LIVE_TRADES=<span class="c-n">0</span></pre></div>\n<h3>3 · Start the indexer</h3>\n<p>Leave this process running. Without it, the trade app can render but its indexed market state will not advance.</p>\n<div class="code"><div class="bar"><span>trade app 1</span><button data-c="bun run indexer/main.ts">copy</button></div><pre><span class="c-c">$</span> bun run indexer/main.ts</pre></div>\n<h3>4 · Start the TradJS trade app</h3>\n<p>Open a second shell in the same repository. Export the port before starting TradJS.</p>\n<div class="code"><div class="bar"><span>trade app 2</span><button data-c="export BUN_PORT=3000\nbunx tradjs server">copy</button></div><pre><span class="c-c">$</span> export BUN_PORT=<span class="c-n">3000</span>\n<span class="c-c">$</span> bunx tradjs server\n<span class="c-c"># open http://localhost:3000</span></pre></div>\n<div class="note"><b>Why two processes?</b> The indexer owns ingestion and local market state. TradJS owns HTTP rendering and browser routes. Starting only the server gives you the interface, but not a running indexer.</div>\n<h3>5 · Use the CLI separately</h3>\n<p>The CLI is an operator interface, not the trade-app launcher. Inspect its commands independently:</p>\n<div class="code"><div class="bar"><span>shell</span><button data-c="solard help">copy</button></div><pre><span class="c-c">$</span> solard help</pre></div>\n<h3>6 · Make the first simulation</h3>\n<div class="code"><div class="bar"><span>shell</span><button data-c="">copy</button></div><pre><span class="c-c">$</span> solard wallet import <span class="c-s">&lt;base58_private_key&gt;</span> --name main\n<span class="c-c">$</span> solard token add <span class="c-s">&lt;token_ca&gt;</span>\n<span class="c-c">$</span> solard quote <span class="c-s">&lt;token_ca&gt;</span> --sol <span class="c-n">0.1</span>\n<span class="c-c">$</span> solard buy <span class="c-s">&lt;token_ca&gt;</span> --wallet main --sol <span class="c-n">0.1</span> --simulate-only</pre></div>\n</section>\n<section data-doc-section="" id="configuration">\n<h2><span class="hash">#</span>Configuration</h2>\n<p>Runtime configuration uses the <code>SOLARD_</code> prefix alongside provider-specific variables. Use the names exactly as shown.</p>\n<table><tr><th>variable</th><th>default / status</th><th>purpose</th></tr>\n<tr><td>SOLARD_DB_PATH</td><td>./solard.db</td><td>Shared SQLite database for CLI, web routes, jobs, portfolio, and indexed state.</td></tr>\n<tr><td>SOLARD_MASTER_KEY</td><td>required for wallets</td><td>Encrypts and decrypts stored wallet secrets.</td></tr>\n<tr><td>HELIUS_RPC_URL</td><td>configure</td><td>Preferred RPC endpoint. <code>RPC_ENDPOINT</code> is accepted for compatibility.</td></tr>\n<tr><td>HELIUS_API_KEY</td><td>optional</td><td>Used to derive the Helius websocket URL when an override is not supplied.</td></tr>\n<tr><td>HELIUS_SENDER_URL</td><td>optional</td><td>Helius Sender fast-lane endpoint.</td></tr>\n<tr><td>HELIUS_TIP_ACCOUNT</td><td>optional</td><td>Tip account for Sender submissions.</td></tr>\n<tr><td>JITO_BLOCK_ENGINE_URL</td><td>sender-specific</td><td>Used only when the Jito sender is explicitly selected.</td></tr>\n<tr><td>SOLARD_ENABLE_LIVE_TRADES</td><td>0</td><td>Rejects live buy, sell, and launch requests until explicitly set to 1.</td></tr>\n<tr><td>SOLARD_WEB_TOKEN</td><td>optional</td><td>When set, browser/API requests send <code>x-solard-web-token</code>.</td></tr>\n<tr><td>SOLARD_PUMP_FEED_SOURCE</td><td>helius</td><td>Selects the primary Pump feed source.</td></tr>\n<tr><td>PUMP_METADATA_PROVIDER</td><td>pump-frontend</td><td>Metadata uploader; <code>pinata</code> requires <code>PINATA_JWT</code>.</td></tr>\n<tr><td>SOLARD_LOG_LEVEL</td><td>info</td><td>Application/measure logging verbosity.</td></tr>\n<tr><td>SOLARD_MAX_JOBS</td><td>100</td><td>Background job retention limit.</td></tr>\n</table>\n<div class="warn"><b>Remote exposure:</b> localhost is the safe default. When binding to a LAN interface, set <code>SOLARD_WEB_TOKEN</code> and prefer a VPN or SSH tunnel over public port forwarding.</div>\n</section>\n<section data-doc-section="" id="architecture">\n<h2><span class="hash">#</span>Architecture</h2>\n<div class="pipe"><i>market sources</i> ──▶ <i>indexer/main.ts</i> ──▶ <i>solard.db</i> ──▶ <i>TradJS server</i> ──▶ <i>browser trade app · CLI · SDK</i></div>\n<h3>Core boundaries</h3>\n<ul class="plain"><li><b>Venue registry:</b> resolves one executable market plugin for a token and user.</li><li><b>Pump plugin:</b> includes bonding-curve and PumpSwap venue implementations, instruction builders, routing, launch sources, and creator-fee claims.</li><li><b>Senders:</b> CLI trade flags expose <code>rpc</code>, <code>helius</code>, and <code>jito</code>.</li><li><b>Persistence:</b> the CLI and web console share a local database.</li><li><b>Observability:</b> worker supervision uses bgrun and operations are instrumented with measure-fn.</li></ul>\n<h3>Repository modules</h3>\n<div class="module-grid"><div><code>src/venues</code><span>market plugins and resolver</span></div><div><code>src/tx</code><span>transaction composition/sending</span></div><div><code>src/claims</code><span>claim-source interfaces</span></div><div><code>src/launches</code><span>launch source abstractions</span></div><div><code>src/runner</code><span>script registration/execution</span></div><div><code>src/solard</code><span>console server, workers, stores</span></div></div>\n</section>\n<section data-doc-section="" id="workers">\n<h2><span class="hash">#</span>Workers</h2>\n<p>A standard start brings up <b>server-worker</b> and <b>helius-logs-v1</b>. Telegram signals joins when <code>SOLARD_TELEGRAM_SIGNALS=1</code>. Additional workers can be enabled for LaserStream, PumpPortal, curve snapshots, holder snapshots, metadata repair, and reconciliation.</p>\n<table><tr><th>worker spec</th><th>role</th><th>default boot?</th></tr>\n<tr><td>solard-server-worker</td><td>SOLARD trade app and API server</td><td>yes</td></tr>\n<tr><td>solard-helius-logs-v1</td><td>standard logs subscription ingestion</td><td>yes</td></tr>\n<tr><td>solard-telegram-signals</td><td>signal ingestion</td><td>when enabled</td></tr>\n<tr><td>solard-helius-live-v2</td><td>Helius live stream path</td><td>registered spec</td></tr>\n<tr><td>solard-helius-laserstream-v1</td><td>transaction subscription / LaserStream path</td><td>registered spec</td></tr>\n<tr><td>solard-pumpportal-live-v2</td><td>PumpPortal stream path</td><td>registered spec</td></tr>\n<tr><td>solard-curve-snapshots</td><td>bonding-account snapshots</td><td>registered spec</td></tr>\n<tr><td>solard-holder-snapshots</td><td>largest-account snapshots</td><td>registered spec</td></tr>\n<tr><td>solard-metadata-repair</td><td>metadata repair loop</td><td>registered spec</td></tr>\n<tr><td>solard-reconciler</td><td>reconciliation and heartbeat</td><td>registered spec</td></tr>\n</table>\n<div class="note"><b>Why this matters:</b> a status screen should not label every registered worker “online” after a default start. The refreshed landing now displays the default boot separately from optional worker specs.</div>\n</section>\n<section data-doc-section="" id="console">\n<h2><span class="hash">#</span>Web console</h2>\n<p>The browser trade app is served by TradJS. Start the indexer first with <code>bun run indexer/main.ts</code>, then run <code>export BUN_PORT=3000</code> and <code>bunx tradjs server</code> in a second shell.</p>\n<div class="route-table"><a href="/trade"><code>/trade</code><span>live trading</span></a><a href="#"><code>/watchlists</code><span>watched tokens, wallets, programs</span></a><a href="#"><code>/signals</code><span>signal projections</span></a><a href="#"><code>/launch</code><span>launch workflow and jobs</span></a><a href="#"><code>/wallets</code><span>wallet and group operations</span></a><a href="#"><code>/portfolio</code><span>portfolio state</span></a><a href="#"><code>/trade</code><span>trade workflow</span></a><a href="#"><code>/activity</code><span>jobs and activity</span></a></div>\n<p>The public <code>/trade</code> route streams token discovery through polling and adds Dexscreener pair metrics as they become available. Wallet and execution routes remain inside the SOLARD console.</p>\n</section>\n<section data-doc-section="" id="api">\n<h2><span class="hash">#</span>HTTP API</h2>\n<p>The repository\'s <code>app/api</code> tree exposes route families for health, status, processes, workers, trade app data, prices, tokens, portfolio, trade, wallet import, groups, watch groups, signals, jobs, launching, and SMA data.</p>\n<table><tr><th>route family</th><th>examples / purpose</th></tr>\n<tr><td>health &amp; status</td><td><code>/api/health</code>, <code>/api/status</code>, process state</td></tr>\n<tr><td>trade app</td><td>Pump feeds, Pump live state, holder data, trade-specific routes</td></tr>\n<tr><td>market</td><td>price and SMA routes</td></tr>\n<tr><td>portfolio</td><td>overview and portfolio routes</td></tr>\n<tr><td>execution</td><td>trade and launch/pump routes</td></tr>\n<tr><td>state changes</td><td>wallet import, groups, watches, worker ensure</td></tr>\n<tr><td>operations</td><td>jobs, history, processes, signals</td></tr>\n</table>\n<div class="note"><b>Authentication:</b> when <code>SOLARD_WEB_TOKEN</code> is set, send it through the <code>x-solard-web-token</code> header.</div>\n</section>\n<section data-doc-section="" id="market-data">\n<h2><span class="hash">#</span>New-token stream &amp; DEX enrichment</h2>\n<p>The server starts token discovery when the Bun process starts. An inline bootstrap polls <code>/api/tokens</code> while the page HTML is loading, before the TradJS page client mounts. The page client then replays buffered events and continues scheduled polling.</p>\n<h3>Source selection</h3>\n<table><tr><th>source</th><th>meaning</th><th>selection</th></tr>\n<tr><td>PumpPortal</td><td>Pump.fun and PumpSwap creation events through <code>subscribeNewToken</code>.</td><td>Used in <code>auto</code> mode when a PumpPortal key or custom websocket URL is configured.</td></tr>\n<tr><td>Solana RPC</td><td>Confirmed Pump create transactions discovered with <code>logsSubscribe</code>, then resolved with <code>getTransaction</code>.</td><td>Keyless default in <code>auto</code> mode. Use a dedicated RPC in production.</td></tr>\n<tr><td>DEX Screener profiles</td><td>Newly published DEX Screener profiles, not every on-chain mint.</td><td>Optional; force with <code>NEW_TOKEN_STREAM_SOURCE=dexscreener</code>.</td></tr>\n</table>\n<h3>Pipeline</h3>\n<div class="pipe"><i>PumpPortal WS or Solana Pump logs</i> ──▶ <i>server buffer</i> ──▶ <i>/api/tokens polling</i> ──▶ <i>landing · trade app</i> ──▶ <i>DEX Screener enrichment</i></div>\n<ol class="doc-steps"><li>Discovery begins with the server process, so arrivals can be buffered before a browser opens.</li><li>The root-layout bootstrap opens polling automatically; no button, scroll, or other interaction starts the stream.</li><li>Each unique mint is returned on the next poll with its creation signature when available.</li><li>An enrichment queue batches up to 30 addresses into DEX Screener token requests.</li><li>Missing pairs are retried with bounded backoff at 5, 15, 30, 60, and 120 seconds.</li><li>When a pair appears, an polling update replaces “pair pending” with price, liquidity, market cap, volume, and transaction counts.</li><li>ETags, bounded backoff, and visibility-aware intervals keep polling predictable and easy to debug.</li></ol>\n<h3>Stream configuration</h3>\n<table><tr><th>variable</th><th>default</th><th>purpose</th></tr>\n<tr><td>NEW_TOKEN_STREAM_SOURCE</td><td>auto</td><td><code>auto</code>, <code>pumpportal</code>, <code>solana-rpc</code>, or <code>dexscreener</code>.</td></tr>\n<tr><td>PUMPPORTAL_API_KEY</td><td>—</td><td>Enables PumpPortal in auto mode; kept on the server.</td></tr>\n<tr><td>PUMPPORTAL_WS_URL</td><td>official endpoint</td><td>Optional credentialed websocket override.</td></tr>\n<tr><td>SOLANA_RPC_URL</td><td>public mainnet RPC</td><td>HTTP endpoint used to resolve create transactions. A dedicated endpoint is recommended.</td></tr>\n<tr><td>SOLANA_WS_URL</td><td>derived/public mainnet WS</td><td>WebSocket endpoint used for Pump-program <code>logsSubscribe</code>.</td></tr>\n<tr><td>DEXSCREENER_PROFILE_WS_URL</td><td>official endpoint</td><td>Optional profile-websocket override for explicit DEX-profile mode.</td></tr>\n<tr><td>DEXSCREENER_PROFILE_POLL_MS</td><td>15000</td><td>REST catch-up interval used only in DEX-profile mode.</td></tr>\n</table>\n<h3>Pair enrichment</h3>\n<p>New mints appear before a DEX pair exists. Until Dexscreener indexes a usable pair, the interface shows <b>pair pending</b> instead of inventing a price or liquidity value.</p>\n<table><tr><th>variable</th><th>default</th><th>purpose</th></tr>\n<tr><td>DEXSCREENER_TOKEN_ADDREpollingS</td><td>—</td><td>Optional Solana watchlist for the pair-metrics view.</td></tr>\n<tr><td>DEXSCREENER_CACHE_MS</td><td>20000</td><td>Snapshot cache duration, clamped between 10 and 120 seconds.</td></tr>\n<tr><td>DEXSCREENER_API_BASE_URL</td><td>official API</td><td>Optional REST proxy or test override.</td></tr>\n</table>\n<div class="warn"><b>Production reliability:</b> Solana’s public RPC can throttle WebSocket subscriptions and transaction lookups. Configure your own Helius or other Solana RPC endpoints for a continuously reliable feed.</div>\n<p>See the <a class="grn" href="https://solana.com/docs/rpc/websocket/logssubscribe">Solana logsSubscribe reference</a>, <a class="grn" href="https://solana.com/docs/rpc/http/gettransaction">getTransaction reference</a>, <a class="grn" href="https://pumpportal.fun/data-api/real-time/">PumpPortal real-time API docs</a>, and <a class="grn" href="https://docs.dexscreener.com/api/reference">DEX Screener API reference</a>.</p>\n</section>\n<section data-doc-section="" id="cli">\n<h2><span class="hash">#</span>CLI reference</h2>\n<p>Run <code>solard help</code> for the current command list. From a source checkout, the equivalent entrypoint is <code>bun src/cli.ts …</code>.</p>\n<h3>Wallets and tokens</h3>\n<div class="code"><div class="bar"><span>solard — wallets and tokens</span></div><pre>solard import <span class="c-s">&lt;private_key&gt;</span> [name]\ncat key.json | solard import --stdin [name]\nsolard wallets [--token <span class="c-s">&lt;token&gt;</span>] [--addresses-only]\nsolard token <span class="c-s">&lt;token_ca&gt;</span> [name] [--metadata-json <span class="c-s">&lt;json&gt;</span>]\nsolard token set <span class="c-s">&lt;token|ca&gt;</span> [--pool <span class="c-s">&lt;address&gt;</span>] [--quote-mint <span class="c-s">&lt;mint&gt;</span>] [--quote-program <span class="c-s">&lt;program&gt;</span>]\nsolard token refresh <span class="c-s">&lt;token|ca&gt;</span>\nsolard tokens</pre></div>\n<h3>Prices and trading</h3>\n<div class="code"><div class="bar"><span>solard — prices and trading</span></div><pre>solard quote buy <span class="c-s">&lt;token|ca&gt;</span> --sol <span class="c-n">0.5</span>\nsolard price <span class="c-s">&lt;token|ca&gt;</span>\nsolard price average <span class="c-s">&lt;token|ca&gt;</span> --period 15m\nsolard price watch <span class="c-s">&lt;token|ca...&gt;</span> [--interval 1s] [--period 1m]\n\nsolard buy <span class="c-s">&lt;token|ca&gt;</span> (--wallet <span class="c-s">&lt;wallet&gt;</span> | --wallets w1,w2 | --group <span class="c-s">&lt;name&gt;</span>) --sol <span class="c-n">0.5</span> [--sender rpc|helius|jito] [--simulate-only]\nsolard sell <span class="c-s">&lt;token|ca&gt;</span> (--wallet <span class="c-s">&lt;wallet&gt;</span> | --wallets w1,w2 | --group <span class="c-s">&lt;name&gt;</span>) [--bps <span class="c-n">10000</span>] [--sender rpc|helius|jito] [--simulate-only]\nsolard unwrap-wsol (--wallet <span class="c-s">&lt;wallet&gt;</span> | --wallets w1,w2 | --group <span class="c-s">&lt;name&gt;</span>) [--simulate-only]\nsolard claim <span class="c-s">&lt;token|ca&gt;</span> --wallet <span class="c-s">&lt;wallet&gt;</span> [--sender rpc|helius|jito]</pre></div>\n<h3>Groups, agents, watches, and ALTs</h3>\n<div class="code"><div class="bar"><span>solard — organization</span></div><pre>solard group create <span class="c-s">&lt;name&gt;</span> [description]\nsolard group add <span class="c-s">&lt;group&gt;</span> <span class="c-s">&lt;wallet&gt;</span> [weight_bps]\nsolard group add-many <span class="c-s">&lt;group&gt;</span> w1,w2,w3\nsolard group show <span class="c-s">&lt;group&gt;</span>\nsolard group list\nsolard agent create <span class="c-s">&lt;name&gt;</span> --wallet <span class="c-s">&lt;wallet&gt;</span> [--config-json <span class="c-s">&lt;json&gt;</span>]\nsolard agent list\nsolard watch token <span class="c-s">&lt;token|ca&gt;</span> [label]\nsolard watch wallet <span class="c-s">&lt;wallet&gt;</span> [label]\nsolard watch program <span class="c-s">&lt;address&gt;</span> [label]\nsolard watch list\nsolard history\nsolard jito tip-accounts [--endpoint <span class="c-s">&lt;url&gt;</span>]\nsolard alt add <span class="c-s">&lt;address&gt;</span> [label]\nsolard alt list\nsolard alt create --wallet <span class="c-s">&lt;wallet&gt;</span>\nsolard alt extend <span class="c-s">&lt;address&gt;</span> --wallet <span class="c-s">&lt;wallet&gt;</span> <span class="c-s">&lt;account...&gt;</span></pre></div>\n</section>\n<section data-doc-section="" id="venues">\n<h2><span class="hash">#</span>Venues &amp; senders</h2>\n<p>The Pump plugin directory contains separate <code>pump-curve-venue.ts</code> and <code>pumpswap-venue.ts</code> implementations plus routing and instruction builders.</p>\n<div class="vgrid"><div class="v"><div class="vt">Pump curve</div><ul><li>bonding-curve venue implementation</li><li>Pump instruction builders</li><li>creator-fee claim source</li><li>launch source and launchpad support</li></ul></div><div class="v"><div class="vt">PumpSwap</div><ul><li>AMM venue implementation</li><li>separate PumpSwap instructions</li><li>resolved through the venue registry</li><li>token pool/quote configuration supported by CLI</li></ul></div></div>\n<h3>Senders</h3>\n<table><tr><th>flag</th><th>path</th><th>use</th></tr><tr><td>rpc</td><td>configured RPC</td><td>default general-purpose sender</td></tr><tr><td>helius</td><td>Helius Sender / configured policy</td><td>fast-lane path when configured</td></tr><tr><td>jito</td><td>Jito block engine</td><td>group buys/sells are documented as bundles of five transactions</td></tr></table>\n<div class="note"><b>Simulation:</b> buy, sell, and unwrap accept <code>--simulate-only</code>. Launching uses its own <code>--live</code> and <code>--skip-simulation</code> controls.</div>\n</section>\n<section data-doc-section="" id="launching">\n<h2><span class="hash">#</span>Launching</h2>\n<p>The CLI supports metadata upload, direct Pump deployment, coordinated buyer groups, and script-driven launch workflows. Use <code>solard deploy pump</code> for direct deployment or a registered script for multi-step workflows.</p>\n<div class="code"><div class="bar"><span>metadata</span></div><pre>solard metadata upload --image <span class="c-s">./logo.png</span> --name <span class="c-s">"TOKEN"</span> --symbol <span class="c-s">TKN</span> --description <span class="c-s">"..."</span> [--provider pump-frontend|pinata]</pre></div>\n<div class="code"><div class="bar"><span>direct deployment</span></div><pre>solard deploy pump --wallet <span class="c-s">dev</span> --name <span class="c-s">"TOKEN"</span> --symbol <span class="c-s">TKN</span> --image <span class="c-s">./logo.png</span> --description <span class="c-s">"..."</span> [--live]</pre></div>\n<div class="code"><div class="bar"><span>workflow</span></div><pre>solard run launch-pump-token --creator <span class="c-s">dev</span> --metadata <span class="c-s">token.json</span> [--creator-buy-sol <span class="c-n">0.1</span>] [--buyer-group <span class="c-s">snipers</span>] [--live]\nsolard run prepare-pump-launch-alt --creator <span class="c-s">dev</span> --metadata <span class="c-s">token.json</span> --creator-buy-sol <span class="c-n">0.1</span> [--create --live]</pre></div>\n<h3>Submit modes</h3>\n<ul class="plain"><li><code>after-deploy-processed</code></li><li><code>spam-after-market-ready</code></li><li><code>fast-spam</code></li></ul>\n</section>\n<section data-doc-section="" id="sdk">\n<h2><span class="hash">#</span>SDK, scripts &amp; agents</h2>\n<p>The package exports the SDK root plus venue, claims, Pump venue, trader preset, launches, launchpads, and runner modules.</p>\n<div class="code"><div class="bar"><span>solard.config.ts</span><button data-c="">copy</button></div><pre><span class="c-k">import</span> { defineSolardConfig } <span class="c-k">from</span> <span class="c-s">"solard/runner"</span>;\n\n<span class="c-k">export default</span> <span class="c-f">defineSolardConfig</span>({\n  scripts: {\n    snipe: <span class="c-s">"./scripts/snipe.ts"</span>,\n    <span class="c-s">"claim-trade-send"</span>: <span class="c-s">"./scripts/claim-trade-send.ts"</span>,\n  },\n});</pre></div>\n<div class="code"><div class="bar"><span>script execution</span></div><pre>solard scripts\nsolard run <span class="c-s">&lt;name-or-path&gt;</span> [script flags...]\nsolard run snipe --name <span class="c-s">"EXACT NAME"</span> --group <span class="c-s">snipers</span> --sol <span class="c-n">0.05</span> --sender jito\nsolard run claim-trade-send --claim <span class="c-s">&lt;token&gt;</span> --buy <span class="c-s">&lt;token&gt;</span> --wallet <span class="c-s">main</span> --recipient <span class="c-s">&lt;address&gt;</span></pre></div>\n<h3>Agent posture</h3>\n<ul class="plain"><li>Use dedicated low-balance wallets.</li><li>Allow read-only quotes and simulations freely.</li><li>Keep <code>SOLARD_ENABLE_LIVE_TRADES=0</code> while developing tool calls.</li><li>Use group weights and agent config as blast-radius controls.</li><li>Inspect history and activity after every automated run.</li></ul>\n</section>\n<section data-doc-section="" id="safety">\n<h2><span class="hash">#</span>Safety &amp; operations</h2>\n<ul class="plain"><li><b>Live gate:</b> shipped as <code>SOLARD_ENABLE_LIVE_TRADES=0</code>.</li><li><b>Wallet encryption:</b> CLI help requires <code>SOLARD_MASTER_KEY</code> to import/decrypt stored wallets.</li><li><b>Simulation:</b> buy, sell, and unwrap support <code>--simulate-only</code>.</li><li><b>Local auth:</b> use <code>SOLARD_WEB_TOKEN</code> when serving beyond localhost.</li><li><b>Supervision:</b> bgrun process state, staleness deadlines, optional auto-recovery.</li><li><b>Tracing:</b> measure-fn instrumentation is present across the codebase.</li></ul>\n<div class="warn"><b>Operational rule:</b> never store a production wallet in a development environment you do not fully control. Start with a disposable wallet, a low balance, and simulations.</div>\n<div class="note"><b>Not financial advice.</b> SOLARD is execution infrastructure for adversarial markets. Review the source, understand every sender and fee policy, and size for total loss.</div>\n</section>\n<section data-doc-section="" id="repository">\n<h2><span class="hash">#</span>Source reference</h2>\n<div class="repo-doc-grid">\n<a href="https://github.com/7flash/solard/blob/master/package.json"><code>package.json</code><span>package name, binary, exports, scripts, dependencies</span></a>\n<a href="https://github.com/7flash/solard/blob/master/indexer/main.ts"><code>indexer/main.ts</code><span>market indexing entry point</span></a>\n<a href="https://github.com/7flash/solard/blob/master/src/cli.ts"><code>src/cli.ts</code><span>canonical command help</span></a>\n<a href="https://github.com/7flash/solard/tree/master/src/venues/pump"><code>src/venues/pump</code><span>Pump curve, PumpSwap, routing, launches, claims</span></a>\n<a href="https://github.com/7flash/solard/blob/master/main.ts"><code>main.ts</code><span>worker registry and default startup selection</span></a>\n<a href="https://github.com/7flash/solard/tree/master/app"><code>app/</code><span>SOLARD trade app routes</span></a>\n<a href="https://github.com/7flash/solard/tree/master/app/api"><code>app/api/</code><span>HTTP API route families</span></a>\n<a href="https://github.com/7flash/solard/blob/master/.env.example"><code>.env.example</code><span>runtime configuration</span></a>\n</div>\n</section>\n<div class="docs-empty" hidden="" id="docs-empty">No documentation sections matched that search.</div>\n</article>\n</div>\n<footer><div class="wrap foot"><div>◧ SOLARD /docs</div><div><a href="/">home</a> · <a href="https://github.com/7flash/solard">github</a> · <a href="https://x.com/solardxyz">x / @solardxyz</a></div></div></footer>';
