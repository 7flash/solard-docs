// Generated from the supplied SOLARD docs mockup.
// Trusted static content rendered by TradJS on the server.
export const docsHtml =
  '<header>\n  <div class="wrap">\n    <nav>\n      <a class="brand" href="/">\n        <svg class="mark" width="18" height="18" viewBox="0 0 32 32" aria-hidden="true">\n          <path d="M10.5 4h16l-3.4 5h-16z"/><path d="M7.1 13.5h16l3.4 5h-16z"/><path d="M10.5 23h16l-3.4 5h-16z"/>\n        </svg>\n        <span class="wm">SOLARD</span><span class="sfx">/docs</span>\n      </a>\n      <div class="nav-links">\n        <a class="docs" href="/">home</a>\n        <a class="gh" href="https://github.com/7flash/solard">github</a>\n        <a class="act" href="#quickstart">quickstart →</a>\n      </div>\n    </nav>\n  </div>\n</header>\n\n<div class="wrap docs">\n\n<aside id="side">\n  <div class="grp">Start here</div>\n  <a href="#overview">Overview</a>\n  <a href="#quickstart">Quickstart</a>\n  <a href="#how">How it works</a>\n  <div class="grp">Interfaces</div>\n  <a href="#cli">CLI reference</a>\n  <a href="#web">Web console</a>\n  <div class="grp">Trading</div>\n  <a href="#venues">Venues &amp; routing</a>\n  <a href="#senders">Senders</a>\n  <a href="#launch">Launching tokens</a>\n  <div class="grp">Extend</div>\n  <a href="#scripts">Scripts &amp; workflows</a>\n  <a href="#agents">AI agents</a>\n  <a href="#api">HTTP API</a>\n  <div class="grp">Reference</div>\n  <a href="#env">Environment</a>\n  <a href="#safety">Safety model</a>\n</aside>\n\n<article>\n\n<!-- OVERVIEW -->\n<section id="overview">\n  <h1>Solard Documentation</h1>\n  <p>Solard is a <b>local-first Solana trading terminal</b> and a <b>multi-wallet CLI/SDK</b> for memecoin traders and AI agents. It runs entirely on your machine: transactions are constructed locally against venue programs directly (no Jupiter, no aggregator), signed with keys that never leave an encrypted local SQLite database, and submitted through your own RPC, Helius Sender, or Jito.</p>\n  <p>There is one binary with two personalities:</p>\n  <ul class="plain">\n    <li><b>solard start</b> — boots the supervised worker fleet and the web console (live pump feed, trading, portfolio, launches, signals).</li>\n    <li><b>solard &lt;command&gt;</b> — the headless CLI: wallets, groups, trades, quotes, launches, scripts. Everything the console does, scriptable.</li>\n  </ul>\n  <p>Both read and write the same local database, so anything you do in one is visible in the other — and in any script or agent you point at it.</p>\n  <div class="note"><b>Design rule:</b> the kernel is boring on purpose. It knows how to talk to venues, sign, send, and persist. Strategy — when to buy, what to snipe, how to exit — lives in <i>your</i> scripts, outside the kernel. The kernel never imports scripts; scripts import the kernel.</div>\n</section>\n\n<!-- QUICKSTART -->\n<section id="quickstart">\n  <h2><span class="hash">#</span>Quickstart</h2>\n  <h3>Requirements</h3>\n  <ul class="plain">\n    <li><b>Bun</b> ≥ 1.1 — solard is Bun-native (SQLite, workers, single-file compile).</li>\n    <li>A <b>Helius API key</b> (free tier works for the feed; paid for LaserStream/Sender).</li>\n    <li>Optional: a Jito block engine endpoint for bundles.</li>\n  </ul>\n  <h3>Install</h3>\n  <div class="code"><div class="bar"><span>shell</span><button data-c="git clone https://github.com/7flash/solard && cd solard && bun install">copy</button></div>\n<pre><span class="c-c">$</span> git clone https://github.com/7flash/solard\n<span class="c-c">$</span> cd solard\n<span class="c-c">$</span> bun install\n<span class="c-c">$</span> cp .env.example .env   <span class="c-c"># then set HELIUS_RPC_URL + SOLARD_MASTER_KEY</span></pre></div>\n  <h3>Boot the console</h3>\n  <div class="code"><div class="bar"><span>shell</span><button data-c="bun bin/solard.ts start --open">copy</button></div>\n<pre><span class="c-c">$</span> bun bin/solard.ts start --open\n<span class="c-c"># workers come online, console opens at http://localhost:3000</span>\n<span class="c-c"># flags: --host 0.0.0.0 --port 4000 to serve on your LAN</span></pre></div>\n  <h3>First (simulated) trade</h3>\n  <div class="code"><div class="bar"><span>shell</span><button data-c="">copy</button></div>\n<pre><span class="c-c">$</span> solard import <span class="c-s">&lt;base58_private_key&gt;</span> main   <span class="c-c"># encrypted with SOLARD_MASTER_KEY</span>\n<span class="c-c">$</span> solard token <span class="c-s">&lt;token_ca&gt;</span> wif              <span class="c-c"># register + fetch metadata</span>\n<span class="c-c">$</span> solard quote buy wif --sol 0.5            <span class="c-c"># inspect the quote, nothing sent</span>\n<span class="c-c">$</span> solard buy wif -w main --sol 0.5 --simulate-only</pre></div>\n  <p>Nothing in the steps above can spend SOL. Live execution is a separate, explicit decision — see <a href="#safety" class="grn">the safety model</a>.</p>\n</section>\n\n<!-- HOW IT WORKS -->\n<section id="how">\n  <h2><span class="hash">#</span>How it works</h2>\n  <p>Solard is a set of <b>independent workers</b> supervised by <b>bgrun</b>, all writing into <b>one local SQLite database</b>, with three read surfaces on top. No worker talks to another worker; the database is the only contract.</p>\n  <div class="pipe">\n    <i>streams</i> (helius-logs · laserstream · pumpportal · curve-snapshots · holder-snapshots) ──▶ <i>solard.db</i> (tokens · trades · holders · indicators · encrypted wallets) ──▶ <i>surfaces</i> (web console · CLI · SDK / scripts / agents)\n  </div>\n  <h3>The worker fleet</h3>\n  <table>\n    <tr><th>worker</th><th>kind</th><th>what it does</th></tr>\n    <tr><td>server-worker</td><td>server</td><td>Serves the web console and the HTTP API.</td></tr>\n    <tr><td>helius-logs</td><td>stream</td><td>Standard <b>logsSubscribe</b> on the pump program; parses create/trade events from logs.</td></tr>\n    <tr><td>helius-laserstream</td><td>stream</td><td><b>transactionSubscribe</b> over LaserStream for full parsed transactions with lower latency.</td></tr>\n    <tr><td>pumpportal-live</td><td>stream</td><td>Independent second opinion on the same market — used to cross-check the Helius feeds.</td></tr>\n    <tr><td>curve-snapshots</td><td>stream</td><td>Polls bonding-curve accounts directly for ground-truth reserves and progress.</td></tr>\n    <tr><td>holder-snapshots</td><td>stream</td><td>Samples largest token accounts on an interval; powers the distribution view.</td></tr>\n    <tr><td>metadata-repair</td><td>stream</td><td>Backfills missing token metadata behind a circuit breaker (fail fast, cool down, retry).</td></tr>\n    <tr><td>reconciler</td><td>reconciler</td><td>Compares feeds, marks stale ingestion, repairs divergence between sources.</td></tr>\n    <tr><td>telegram-signals</td><td>signals</td><td>Polls configured Telegram channels and projects calls into the signals table.</td></tr>\n  </table>\n  <h3>Supervision &amp; tracing</h3>\n  <p>Every worker registers with bgrun with a <b>staleness deadline</b> — if a stream stops writing heartbeats, it\'s restarted, and the console shows it as degraded before your positions notice. Every operation is traced end-to-end with <b>measure-fn</b>: quotes, sends, confirmations, feed ingestion — all have timing spans you can inspect when something is slow.</p>\n  <h3>Why multiple feeds for the same market?</h3>\n  <p>Because each one lies differently. Log parsing is cheap but drops events under load; LaserStream is complete but is a different subscription model; PumpPortal is someone else\'s infrastructure. The reconciler\'s job is to notice when they disagree, and the <b>live doctor</b> (<code>/api/terminal/doctor</code>) will tell you exactly which feed is stale and since when.</p>\n</section>\n\n<!-- CLI -->\n<section id="cli">\n  <h2><span class="hash">#</span>CLI reference</h2>\n  <p>Everything below persists into the shared database and is immediately visible in the web console. Run <code>solard help</code> for the always-current version of this list.</p>\n\n  <h3>Wallets &amp; tokens</h3>\n  <div class="code"><div class="bar"><span>solard — wallets &amp; tokens</span></div>\n<pre>solard import <span class="c-s">&lt;private_key&gt;</span> [name]          <span class="c-c"># encrypted at rest</span>\ncat key.json | solard import --stdin [name]\nsolard wallets [--token <span class="c-s">&lt;token&gt;</span>] [--addresses-only]\nsolard token <span class="c-s">&lt;token_ca&gt;</span> [name] [--metadata-json <span class="c-s">&lt;json&gt;</span>]\nsolard token set <span class="c-s">&lt;token|ca&gt;</span> [--pool <span class="c-s">&lt;address&gt;</span>] [--quote-mint <span class="c-s">&lt;mint&gt;</span>]\nsolard token refresh <span class="c-s">&lt;token|ca&gt;</span>\nsolard tokens</pre></div>\n\n  <h3>Groups</h3>\n  <div class="code"><div class="bar"><span>solard — groups</span></div>\n<pre>solard group create <span class="c-s">&lt;name&gt;</span> [description]\nsolard group add <span class="c-s">&lt;group&gt;</span> <span class="c-s">&lt;wallet&gt;</span> [weight_bps]   <span class="c-c"># weighted sizing</span>\nsolard group add-many <span class="c-s">&lt;group&gt;</span> w1,w2,w3\nsolard group show <span class="c-s">&lt;group&gt;</span>\nsolard group list</pre></div>\n\n  <h3>Prices &amp; quotes</h3>\n  <div class="code"><div class="bar"><span>solard — prices</span></div>\n<pre>solard quote buy <span class="c-s">&lt;token|ca&gt;</span> --sol <span class="c-n">0.5</span>      <span class="c-c"># inspect without submitting</span>\nsolard price <span class="c-s">&lt;token|ca&gt;</span>                    <span class="c-c"># sample current venue price</span>\nsolard price average <span class="c-s">&lt;token|ca&gt;</span> --period 15m\nsolard price watch <span class="c-s">&lt;token...&gt;</span> [--interval 1s] [--period 1m]</pre></div>\n\n  <h3>Trading</h3>\n  <div class="code"><div class="bar"><span>solard — trading</span></div>\n<pre>solard buy <span class="c-s">&lt;token|ca&gt;</span> (-w <span class="c-s">&lt;wallet&gt;</span> | --wallets w1,w2 | --group <span class="c-s">&lt;name&gt;</span>)\n           --sol <span class="c-n">0.5</span> [--slippage-bps <span class="c-n">1500</span>] [--sender rpc|helius|jito] [--simulate-only]\n\nsolard sell <span class="c-s">&lt;token|ca&gt;</span> (-w <span class="c-s">&lt;wallet&gt;</span> | --wallets w1,w2 | --group <span class="c-s">&lt;name&gt;</span>)\n            [--bps <span class="c-n">10000</span>] [--slippage-bps <span class="c-n">1500</span>] [--sender rpc|helius|jito] [--simulate-only]\n\nsolard claim <span class="c-s">&lt;token|ca&gt;</span> -w <span class="c-s">&lt;wallet&gt;</span>          <span class="c-c"># pump creator fees</span>\nsolard unwrap-wsol (-w <span class="c-s">&lt;wallet&gt;</span> | --group <span class="c-s">&lt;name&gt;</span>)</pre></div>\n  <p>Exactly one of <code>--wallet</code>, <code>--wallets</code>, or <code>--group</code> must be supplied. Group trades over <code>--sender jito</code> are packed as bundles, <b>five transactions per bundle</b>. <code>--bps 10000</code> on sell means the full position; <code>--bps 5000</code> is half.</p>\n\n  <h3>Watching &amp; transactions</h3>\n  <div class="code"><div class="bar"><span>solard — watch / history / ALTs</span></div>\n<pre>solard watch token <span class="c-s">&lt;token|ca&gt;</span> [label]\nsolard watch wallet <span class="c-s">&lt;wallet&gt;</span> [label]\nsolard watch program <span class="c-s">&lt;address&gt;</span> [label]\nsolard watch list\nsolard history\nsolard jito tip-accounts\nsolard alt create -w <span class="c-s">&lt;wallet&gt;</span>               <span class="c-c"># address lookup tables</span>\nsolard alt extend <span class="c-s">&lt;address&gt;</span> -w <span class="c-s">&lt;wallet&gt;</span> <span class="c-s">&lt;account...&gt;</span></pre></div>\n\n  <h3>Scripts &amp; workers</h3>\n  <div class="code"><div class="bar"><span>solard — scripts / workers</span></div>\n<pre>solard scripts                              <span class="c-c"># list solard.config.ts registrations</span>\nsolard run <span class="c-s">&lt;name-or-path&gt;</span> [flags...]\nsolard workers status                       <span class="c-c"># fleet health from the CLI</span>\nsolard workers ensure                       <span class="c-c"># start anything that\'s down</span></pre></div>\n</section>\n\n<!-- WEB -->\n<section id="web">\n  <h2><span class="hash">#</span>Web console</h2>\n  <p><code>solard start --open</code> serves the console at <code>http://localhost:3000</code>. It is a thin, fast view over the same database the CLI writes — SSR-rendered, keyboard-driven, no build step.</p>\n  <table>\n    <tr><th>page</th><th>what\'s on it</th></tr>\n    <tr><td>/terminal</td><td>The live pump feed: new creates, trades, curve progress, SMA indicators, per-token holder distribution, feed-health strip.</td></tr>\n    <tr><td>/trade</td><td>Buy/sell forms against any registered token, wallet/group selector, sender choice, quote preview, simulation results before anything is live.</td></tr>\n    <tr><td>/portfolio</td><td>Positions across all wallets and groups: cost basis, current venue price, unrealized PnL, claimable creator fees.</td></tr>\n    <tr><td>/wallets</td><td>Import, group management, weights, batch SOL balances, batch fund/sweep.</td></tr>\n    <tr><td>/watchlists</td><td>Watched tokens, wallets, and programs with live activity.</td></tr>\n    <tr><td>/launch</td><td>Full pump.fun launch flow: metadata upload, vanity mint, buyer-group coordination, submit mode — as a form. Launches run as background jobs with step-by-step logs.</td></tr>\n    <tr><td>/signals</td><td>Telegram signal projections mapped onto live token state.</td></tr>\n    <tr><td>/activity</td><td>Everything solard has executed: sends, confirmations, errors, with measure-fn traces.</td></tr>\n  </table>\n  <h3>Auth</h3>\n  <p>The console binds to localhost by default and needs no auth. If you serve it beyond localhost (<code>--host 0.0.0.0</code>), set <code>SOLARD_WEB_TOKEN</code> — every browser and API call must then send it as the <code>x-solard-web-token</code> header.</p>\n  <div class="warn"><b>Exposure warning:</b> the console can see wallet balances and (behind the live gate) trigger trades. Don\'t port-forward it to the internet; use a VPN or SSH tunnel if you need remote access.</div>\n</section>\n\n<!-- VENUES -->\n<section id="venues">\n  <h2><span class="hash">#</span>Venues &amp; routing</h2>\n  <p>A <b>venue</b> is anywhere a token can be traded. Solard ships two venue plugins and constructs their instructions manually — no aggregator in the path, which means no aggregator fees, no extra CPI depth, and quotes computed from on-chain state you fetched yourself.</p>\n  <div class="vgrid">\n    <div class="v">\n      <div class="vt">pump-curve</div>\n      <ul>\n        <li>pump.fun bonding curve, pre-migration</li>\n        <li>quote = exact curve math from the bonding account</li>\n        <li>buy/sell + creator-fee claims</li>\n        <li>launch deployments happen here</li>\n      </ul>\n    </div>\n    <div class="v">\n      <div class="vt">pumpswap-amm</div>\n      <ul>\n        <li>PumpSwap pool, post-migration</li>\n        <li>quote = constant-product from pool reserves</li>\n        <li>buy/sell via direct AMM CPIs</li>\n        <li>pool + quote mint overridable per token</li>\n      </ul>\n    </div>\n  </div>\n  <h3>The route resolver</h3>\n  <p>You never pick a venue by hand. For each trade the resolver reads the token\'s on-chain state: if the bonding curve is live and incomplete, the trade routes to <b>pump-curve</b>; once the token has migrated, it routes to <b>pumpswap-amm</b> using the stored (or auto-discovered) pool. The decision happens at quote time, so a token that migrates between your quote and your send fails loudly instead of trading against a dead curve.</p>\n  <p>Venues are a plugin interface — adding a new one means implementing <code>quoteBuy</code> / <code>quoteSell</code> / instruction builders, and every workflow, script, and console form gets it for free.</p>\n</section>\n\n<!-- SENDERS -->\n<section id="senders">\n  <h2><span class="hash">#</span>Senders</h2>\n  <p>Venue plugins produce instructions; <b>senders</b> get them on chain. Pick per trade with <code>--sender</code>.</p>\n  <table>\n    <tr><th>sender</th><th>path</th><th>when to use</th></tr>\n    <tr><td>rpc</td><td>Your RPC\'s <b>sendTransaction</b> with priority fees.</td><td>Default. Cheap, fine for anything that isn\'t a race.</td></tr>\n    <tr><td>helius</td><td>Helius <b>Sender</b> fast lane with a tip account.</td><td>Latency-sensitive single transactions — snipes, exits. Configure <code>HELIUS_SENDER_URL</code> + <code>HELIUS_TIP_ACCOUNT</code>.</td></tr>\n    <tr><td>jito</td><td>Bundles via a Jito block engine.</td><td>Atomic group execution: 5 tx per bundle, all-or-nothing per bundle. Group buys/sells and coordinated launch buys.</td></tr>\n  </table>\n  <p>Every sender path supports simulation first; <code>--simulate-only</code> runs the full pipeline — quote, build, simulate — and stops before broadcast, printing compute units and balance changes.</p>\n</section>\n\n<!-- LAUNCH -->\n<section id="launch">\n  <h2><span class="hash">#</span>Launching tokens</h2>\n  <p>A pump.fun launch in solard is one command coordinating three phases: <b>metadata → deployment → buyer wave</b>.</p>\n  <div class="code"><div class="bar"><span>shell</span></div>\n<pre><span class="c-c">$</span> solard launch pump --creator dev \\\n    --image ./logo.png --description <span class="c-s">"..."</span> \\\n    --buyer-group snipers \\\n    --submit-mode spam-after-market-ready \\\n    --sender-tps <span class="c-n">40</span> --helius-tip-sol <span class="c-n">0.01</span> \\\n    --live</pre></div>\n  <h3>Metadata</h3>\n  <p>Uploaded to IPFS through the <b>pump frontend endpoint</b> by default, or <b>Pinata</b> with <code>PUMP_METADATA_PROVIDER=pinata</code> + a JWT. Twitter/Telegram/website/video links ride along; <code>--hide-name</code> supported. You can also pre-upload with <code>solard metadata upload</code> and pass <code>--uri</code>.</p>\n  <h3>Vanity mints</h3>\n  <p>The launcher can grind a mint keypair whose address ends in your chosen suffix (multi-threaded, runs while you prepare metadata), so your CA reads like <code>...pump</code> or your ticker.</p>\n  <h3>Submit modes</h3>\n  <table>\n    <tr><th>mode</th><th>behavior</th></tr>\n    <tr><td>after-deploy-processed</td><td>Wait for the deploy tx to reach <b>processed</b>, then send buyer transactions. Safest, default.</td></tr>\n    <tr><td>spam-after-market-ready</td><td>Start firing buys the instant the curve account exists, resending at <code>--sender-tps</code> until confirmed. For competitive launches.</td></tr>\n    <tr><td>fast-spam</td><td>Fire buys alongside the deploy without waiting. Maximum aggression, expect some failures.</td></tr>\n  </table>\n  <p>Buyer-group transactions are pre-built against a prepared <b>address lookup table</b> (<code>solard run prepare-pump-launch-alt</code>) so they fit and land smaller. Every launch runs as a background job — watch it on <code>/launch</code> or tail the CLI output.</p>\n</section>\n\n<!-- SCRIPTS -->\n<section id="scripts">\n  <h2><span class="hash">#</span>Scripts &amp; workflows</h2>\n  <p>Your strategy is a TypeScript file that imports the SDK. Register it in <code>solard.config.ts</code>, run it with <code>solard run</code>, pass it flags like any command.</p>\n  <div class="code"><div class="bar"><span>solard.config.ts</span></div>\n<pre><span class="c-k">import</span> { defineSolardConfig } <span class="c-k">from</span> <span class="c-s">"solard/runner"</span>;\n\n<span class="c-k">export default</span> <span class="c-f">defineSolardConfig</span>({\n  scripts: {\n    snipe: <span class="c-s">"./scripts/snipe.ts"</span>,\n    dca:   { path: <span class="c-s">"./scripts/dca.ts"</span>, description: <span class="c-s">"laddered entries"</span> },\n  },\n});</pre></div>\n  <div class="code"><div class="bar"><span>scripts/snipe.ts</span></div>\n<pre><span class="c-k">import</span> { openSolard } <span class="c-k">from</span> <span class="c-s">"solard"</span>;\n\n<span class="c-k">const</span> solard = <span class="c-k">await</span> <span class="c-f">openSolard</span>();          <span class="c-c">// shared db + env</span>\n\n<span class="c-c">// wait for a launch matching a name, then buy across a group</span>\n<span class="c-k">const</span> launch = <span class="c-k">await</span> solard.launches.<span class="c-f">waitFor</span>({ name: <span class="c-s">"EXACT NAME"</span> });\n<span class="c-k">const</span> receipt = <span class="c-k">await</span> solard.<span class="c-f">buy</span>({\n  token: launch.mint,\n  group: <span class="c-s">"snipers"</span>,\n  sol: <span class="c-n">0.05</span>,\n  sender: <span class="c-s">"jito"</span>,\n  live: process.argv.includes(<span class="c-s">"--live"</span>),   <span class="c-c">// simulate by default</span>\n});\nconsole.log(receipt.signatures);</pre></div>\n  <h3>Built-in workflows</h3>\n  <table>\n    <tr><th>workflow</th><th>what it composes</th></tr>\n    <tr><td>snipe</td><td>Launch discovery → group buy the moment the market exists.</td></tr>\n    <tr><td>claim-trade-send</td><td>Claim creator fees from any claim source → buy a token quoted in the same asset → deliver the guaranteed minimum output to a recipient. Venue-agnostic.</td></tr>\n    <tr><td>wait-launch-trade-group</td><td>Block on a launch source, then execute a coordinated group entry with per-wallet sizing.</td></tr>\n  </table>\n  <p>Workflows are plugins too — they receive a host with <code>resolveToken</code>, <code>route</code>, <code>signer</code>, and the sender registry, so they work on any venue that exists now or later.</p>\n</section>\n\n<!-- AGENTS -->\n<section id="agents">\n  <h2><span class="hash">#</span>AI agents</h2>\n  <p>Solard was built to be driven by agents as much as by humans. There are <b>three integration surfaces</b>, in order of tightness. In all three, the <a href="#safety" class="grn">safety gate</a> stays in charge: an agent cannot trade live unless you flipped <code>SOLARD_ENABLE_LIVE_TRADES=1</code>, no matter what it asks for.</p>\n\n  <h3>1 · The SDK — for TypeScript agents</h3>\n  <p>If your agent runs in Bun/Node, import solard directly. Everything is typed: wallet refs, token refs, group refs, quotes, receipts. This is the same code path the CLI uses.</p>\n  <div class="code"><div class="bar"><span>agent.ts</span></div>\n<pre><span class="c-k">import</span> { openSolard } <span class="c-k">from</span> <span class="c-s">"solard"</span>;\n\n<span class="c-k">const</span> solard = <span class="c-k">await</span> <span class="c-f">openSolard</span>();\n\n<span class="c-c">// register the agent — its actions are attributed in activity logs</span>\n<span class="c-k">const</span> agent = <span class="c-k">await</span> solard.agents.<span class="c-f">create</span>(<span class="c-s">"scout-1"</span>, {\n  wallet: <span class="c-s">"agent-hot"</span>,\n  config: { maxSolPerTrade: <span class="c-n">0.25</span>, allowedVenues: [<span class="c-s">"pump-curve"</span>] },\n});\n\n<span class="c-c">// read market state the workers have already indexed</span>\n<span class="c-k">const</span> feed = <span class="c-k">await</span> solard.terminal.<span class="c-f">feed</span>({ limit: <span class="c-n">50</span> });\n<span class="c-k">const</span> holders = <span class="c-k">await</span> solard.terminal.<span class="c-f">holders</span>(feed[<span class="c-n">0</span>].mint);\n\n<span class="c-c">// quote → decide → execute (simulated unless the gate is open)</span>\n<span class="c-k">const</span> quote = <span class="c-k">await</span> solard.<span class="c-f">quoteBuy</span>(feed[<span class="c-n">0</span>].mint, { sol: <span class="c-n">0.1</span> });\n<span class="c-k">if</span> (quote.priceImpactBps &lt; <span class="c-n">300</span>) {\n  <span class="c-k">await</span> agent.<span class="c-f">buy</span>({ token: feed[<span class="c-n">0</span>].mint, sol: <span class="c-n">0.1</span>, sender: <span class="c-s">"helius"</span> });\n}</pre></div>\n\n  <h3>2 · The CLI — for tool-calling agents</h3>\n  <p>For LLM agents that execute shell tools (Claude Code, custom harnesses), the CLI <i>is</i> the tool set. Every command reads flags, prints structured output, and exits nonzero on failure — exactly what a tool loop wants. A minimal tool manifest:</p>\n  <div class="code"><div class="bar"><span>tools for an LLM agent</span></div>\n<pre>solard tokens                       <span class="c-c"># list what\'s registered</span>\nsolard price <span class="c-s">&lt;token&gt;</span>                <span class="c-c"># current venue price</span>\nsolard quote buy <span class="c-s">&lt;token&gt;</span> --sol X    <span class="c-c"># read-only quote</span>\nsolard buy ... --simulate-only      <span class="c-c"># full dry-run with balance deltas</span>\nsolard buy ...                      <span class="c-c"># only works if the gate is open</span>\nsolard history                      <span class="c-c"># what happened</span></pre></div>\n  <div class="note"><b>Recommended agent posture:</b> give the agent <code>quote</code>, <code>price</code>, and <code>--simulate-only</code> freely; require human confirmation (or a size-capped agent wallet) for anything live. The <code>weight_bps</code> on group membership and a dedicated low-balance agent wallet are your blast-radius controls.</div>\n\n  <h3>3 · The HTTP API — for anything else</h3>\n  <p>Python agents, remote orchestrators, anything that speaks HTTP — the console\'s API is a complete surface. See <a href="#api" class="grn">the endpoint list</a>. Authenticate with <code>x-solard-web-token</code> when the token is set.</p>\n  <div class="code"><div class="bar"><span>python</span></div>\n<pre><span class="c-k">import</span> requests\n\nS = <span class="c-s">"http://localhost:3000"</span>\nH = {<span class="c-s">"x-solard-web-token"</span>: token}\n\nfeed  = requests.get(f<span class="c-s">"{S}/api/terminal/feed"</span>, headers=H).json()\nquote = requests.post(f<span class="c-s">"{S}/api/trade/buy"</span>, headers=H, json={\n    <span class="c-s">"token"</span>: feed[<span class="c-s">"data"</span>][<span class="c-n">0</span>][<span class="c-s">"mint"</span>],\n    <span class="c-s">"wallet"</span>: <span class="c-s">"agent-hot"</span>,\n    <span class="c-s">"sol"</span>: <span class="c-n">0.1</span>,\n    <span class="c-s">"live"</span>: <span class="c-k">False</span>,          <span class="c-c"># simulation; live=True requires the env gate</span>\n}).json()</pre></div>\n</section>\n\n<!-- API -->\n<section id="api">\n  <h2><span class="hash">#</span>HTTP API</h2>\n  <p>All routes are served by the console server. Responses use a uniform envelope: <code>{ ok, data }</code> on success, <code>{ ok: false, error: { code, message } }</code> on failure.</p>\n  <table>\n    <tr><th>route</th><th>method</th><th>purpose</th></tr>\n    <tr><td>/api/status</td><td>GET</td><td>Server + gate status, versions, db path.</td></tr>\n    <tr><td>/api/health · /api/terminal/health</td><td>GET</td><td>Worker fleet health with staleness per stream.</td></tr>\n    <tr><td>/api/terminal/doctor</td><td>GET</td><td>Deep feed diagnosis — which source is stale, last event per feed.</td></tr>\n    <tr><td>/api/terminal/feed · /api/pump-live</td><td>GET</td><td>Live pump feed: creates, trades, curve progress.</td></tr>\n    <tr><td>/api/terminal/trades</td><td>GET</td><td>Trade stream for a mint.</td></tr>\n    <tr><td>/api/terminal/curve</td><td>GET</td><td>Bonding-curve snapshots for a mint.</td></tr>\n    <tr><td>/api/terminal/holders · /api/token-holders</td><td>GET</td><td>Holder distribution series (the terminal chart\'s data).</td></tr>\n    <tr><td>/api/market/sma</td><td>GET</td><td>SMA indicator values.</td></tr>\n    <tr><td>/api/price</td><td>GET</td><td>Venue price for a registered token.</td></tr>\n    <tr><td>/api/portfolio · /api/overview</td><td>GET</td><td>Positions, balances, PnL across wallets/groups.</td></tr>\n    <tr><td>/api/wallets/import</td><td>POST</td><td>Import a wallet (encrypted at rest).</td></tr>\n    <tr><td>/api/groups/create · /api/groups/add</td><td>POST</td><td>Group management.</td></tr>\n    <tr><td>/api/trade/buy · /api/trade/sell</td><td>POST</td><td>Execute or simulate a trade; <b>live:true</b> rejected unless the gate is open.</td></tr>\n    <tr><td>/api/launch/pump</td><td>POST</td><td>Start a launch job; poll <b>/api/jobs</b> for progress.</td></tr>\n    <tr><td>/api/signals · /api/terminal/signals</td><td>GET</td><td>Telegram signal projections.</td></tr>\n    <tr><td>/api/processes · /api/workers/ensure</td><td>GET/POST</td><td>Inspect and (re)start the worker fleet.</td></tr>\n  </table>\n</section>\n\n<!-- ENV -->\n<section id="env">\n  <h2><span class="hash">#</span>Environment</h2>\n  <table>\n    <tr><th>variable</th><th>default</th><th>meaning</th></tr>\n    <tr><td>SOLARD_DB_PATH</td><td>./solard.db</td><td>The shared SQLite database — wallets, tokens, feed, everything.</td></tr>\n    <tr><td>SOLARD_MASTER_KEY</td><td>—</td><td><b>Required to import/decrypt wallets.</b> Lose it and stored keys are unrecoverable.</td></tr>\n    <tr><td>HELIUS_RPC_URL</td><td>—</td><td>Primary RPC. <code>RPC_ENDPOINT</code> accepted for compatibility.</td></tr>\n    <tr><td>HELIUS_API_KEY</td><td>—</td><td>Used to derive the websocket URL for stream workers.</td></tr>\n    <tr><td>HELIUS_SENDER_URL</td><td>—</td><td>Helius Sender fast-lane endpoint (regional or global).</td></tr>\n    <tr><td>HELIUS_TIP_ACCOUNT</td><td>—</td><td>Tip account for Sender submissions.</td></tr>\n    <tr><td>JITO_BLOCK_ENGINE_URL</td><td>mainnet.block-engine.jito.wtf</td><td>Only when explicitly selecting the jito sender.</td></tr>\n    <tr><td>SOLARD_ENABLE_LIVE_TRADES</td><td><b>0</b></td><td>The live gate. Everything is simulation until this is 1.</td></tr>\n    <tr><td>SOLARD_WEB_TOKEN</td><td>—</td><td>When set, all console/API calls must send <code>x-solard-web-token</code>.</td></tr>\n    <tr><td>SOLARD_PUMP_FEED_SOURCE</td><td>helius</td><td>Primary feed source for the terminal.</td></tr>\n    <tr><td>PUMP_METADATA_PROVIDER</td><td>pump-frontend</td><td>IPFS provider for launch metadata; <code>pinata</code> needs <code>PINATA_JWT</code>.</td></tr>\n    <tr><td>SOLARD_LOG_LEVEL</td><td>info</td><td>measure-fn log verbosity.</td></tr>\n    <tr><td>SOLARD_MAX_JOBS</td><td>100</td><td>Background job retention (launches).</td></tr>\n    <tr><td>SOLARD_METADATA_CIRCUIT_*</td><td>5 / 60000 / 10000</td><td>Metadata-repair circuit breaker: failures to open, open ms, half-open retry ms.</td></tr>\n  </table>\n</section>\n\n<!-- SAFETY -->\n<section id="safety">\n  <h2><span class="hash">#</span>Safety model</h2>\n  <p>Solard assumes you will eventually connect something impulsive to it — a 4am you, or an agent. The defaults are built for that.</p>\n  <ul class="plain">\n    <li><b>Dry-run by default.</b> <code>SOLARD_ENABLE_LIVE_TRADES=0</code> ships in <code>.env.example</code>. Live buy, sell, and launch — CLI, console, and API alike — reject <code>live=true</code> until you flip it. Simulation runs the entire pipeline and reports balance deltas.</li>\n    <li><b>Keys encrypted at rest.</b> Wallet secrets are encrypted with <code>SOLARD_MASTER_KEY</code> before touching disk. The key itself lives only in your environment.</li>\n    <li><b>Simulate anything.</b> <code>--simulate-only</code> works on every trade path including group bundles, printing compute and slippage before you commit.</li>\n    <li><b>Blast-radius controls for agents.</b> Dedicated low-balance agent wallets, per-wallet <code>weight_bps</code> in groups, and per-agent config caps.</li>\n    <li><b>Local by default.</b> The console binds localhost. Widening it is an explicit flag, and a token gate exists for when you do.</li>\n    <li><b>Everything attributable.</b> Every execution is logged with its origin (CLI, console, script, agent) and its full measure-fn trace on <code>/activity</code>.</li>\n  </ul>\n  <div class="note"><b>Not financial advice.</b> Solard is execution infrastructure. Memecoin markets are adversarial; simulate first, size for loss, and read the code — that\'s why it\'s open.</div>\n</section>\n\n</article>\n</div>\n\n<footer>\n  <div class="wrap foot">\n    <div>◧ SOLARD /docs</div>\n    <div><a href="/">home</a> · <a href="https://github.com/7flash/solard">github</a></div>\n  </div>\n</footer>';
